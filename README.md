## Instructions

1. Copy the files in `/init/` into the home directory of your machine. These will point `bash` and Emacs to the real version controlled config files
2. Add a file `.localaliases` into this repository to map whatever aliases are most relevant for the machine you are using.
